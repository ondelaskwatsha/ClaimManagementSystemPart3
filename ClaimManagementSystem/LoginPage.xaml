<Page x:Class="ClaimManagementSystem.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Login">
    <Grid Background="#f8f9fa">
        <Border Width="400" Padding="30" Background="Maroon" 
                BorderBrush="#ddd" BorderThickness="1" 
                HorizontalAlignment="Center" VerticalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Welcome Back" FontSize="24" FontWeight="Bold" 
                          HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <TextBlock Text="Email" FontWeight="SemiBold" Margin="0,10,0,5"/>
                <TextBox x:Name="txtEmail" Padding="10"/>

                <TextBlock Text="Password" FontWeight="SemiBold" Margin="0,10,0,5"/>
                <PasswordBox x:Name="txtPassword" Padding="10"/>

                <Button Content="Sign In" Click="BtnLogin_Click" 
                        Background="#3498db" Foreground="White" 
                        FontWeight="Bold" Padding="15,10" Margin="0,20,0,10"/>

                <TextBlock Text="Don't have an account? Sign up" 
                          HorizontalAlignment="Center" Foreground="WhiteSmoke"
                          MouseDown="TextBlock_SignUp_MouseDown">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Foreground" Value="#3498db"/>
                                    <Setter Property="TextDecorations" Value="Underline"/>
                                    <Setter Property="Cursor" Value="Hand"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</Page>